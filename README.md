# unlock
PDFのパスワードを解除する。\n
Remove the PDF password.

# split
PDFを指定のページで分割する。\n
Split a PDF at a specified page.

# compression
PDFを圧縮する。\n
Compress PDFs.

# preparation
```
pip install pypdf2
sudo apt install ghostscript
```